# Universal AI Agent Instructions

- Prioritize preserving the B2B, corporate fitness tone defined in the README and VISION.md. Never use "arcade" or "video game" terminology when drafting public-facing assets.
- Ensure any user-facing version display references the same version source (`VERSION.md`), rather than duplicating hard-coded version strings.
- Do not hard-code, print, log, or commit API keys or secrets.
- Add comments only where they explain non-obvious logic, architecture decisions, side effects, tradeoffs, or known limitations. Do not comment obvious code.
- If adding dependencies, justify them in `HANDOFF.md`.
- Read existing documentation before making architectural changes.